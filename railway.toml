[build]
builder = "nixpacks"
buildCommand = "apt-get update && apt-get install -y docker.io docker-compose"

[deploy]
startCommand = "make infrastructure-up"
healthcheckPath = "/docs"
healthcheckTimeout = 100

[env]
PYTHON_VERSION = "3.11"