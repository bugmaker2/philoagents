[build]
builder = "nixpacks"
buildCommand = "cd philoagents-api && pip install uv && uv venv .venv && . .venv/bin/activate && uv pip install -e ."

[deploy]
startCommand = "make infrastructure-up"
healthcheckPath = "/docs"
healthcheckTimeout = 100

[env]
PYTHON_VERSION = "3.11"